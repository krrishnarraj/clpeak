# C/C++ with GCC
# Build your C/C++ project with GCC using make.
# Add steps that publish test results, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/c-cpp/gcc

trigger:
- master

pool:
  vmImage: 'vs2017-win2016'

variables:
  OCL_ROOT: '$(%TEMP%/ocl_sdk)'

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: 'New-Item -ItemType Directory -Force -Path $env:temp/ocl_sdk; echo "step 1"; wget https://github.com/GPUOpen-LibrariesAndSDKs/OCL-SDK/files/1406216/lightOCLSDK.zip -outfile $env:temp/ocl_sdk.zip; echo "step 2"; Expand-Archive $env:temp/ocl_sdk.zip -DestinationPath $env:temp/ocl_sdk/; echo "step 3"'
    filePath: ''

- task: CMake@1
  inputs:
    workingDirectory: 'build'
    cmakeArgs: '..'
  displayName: 'cmake'
